# server-side-common

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build server-side-common` to build the library.

## Running unit tests

Run `nx test server-side-common` to execute the unit tests via [Jest](https://jestjs.io).
